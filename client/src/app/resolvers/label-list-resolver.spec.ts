import { LabelListResolver } from './label-list-resolver';

describe('LabelListResolver', () => {
  it('should create an instance', () => {
    expect(new LabelListResolver()).toBeTruthy();
  });
});
