import { WeightUnitListResolver } from './weightUnit-list-resolver';

describe('WeightUnitListResolver', () => {
  it('should create an instance', () => {
    expect(new WeightUnitListResolver()).toBeTruthy();
  });
});
