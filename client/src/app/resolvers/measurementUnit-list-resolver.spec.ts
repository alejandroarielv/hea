import { MeasurementUnitListResolver } from './measurementUnit-list-resolver';

describe('MeasurementUnitListResolver', () => {
  it('should create an instance', () => {
    expect(new MeasurementUnitListResolver()).toBeTruthy();
  });
});
