import { ShippingTypeListResolver } from './shippingType-list-resolver';

describe('ShippingTypeListResolver', () => {
  it('should create an instance', () => {
    expect(new ShippingTypeListResolver()).toBeTruthy();
  });
});
