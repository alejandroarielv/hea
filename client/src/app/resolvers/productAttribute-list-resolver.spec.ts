import { ProductAttributeListResolver } from './productAttribute-list-resolver';

describe('ProductAttributeListResolver', () => {
  it('should create an instance', () => {
    expect(new ProductAttributeListResolver()).toBeTruthy();
  });
});
